apiVersion: v1
kind: Service
metadata:
  name: taskline-frontend-service
  labels:
    app: taskline-frontend
spec:
  type: NodePort
  selector:
    app: taskline-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
