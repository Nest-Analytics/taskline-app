apiVersion: v1
kind: Service
metadata:
  name: tasklineapp-service
  labels:
    app: tasklineapp
spec:
  type: NodePort
  selector:
    app: tasklineapp
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
